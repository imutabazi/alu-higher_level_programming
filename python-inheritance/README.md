# Python - Inheritance

This repository contains solutions for the ALX tasks focusing on Python Object-Oriented Programming, specifically inheritance, subclassing, overriding methods, and built-in functions like `isinstance`, `issubclass`, `type`, and `dir`.
